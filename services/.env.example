# --------------------------------------------------------------------------
# Authentication: DefaultAzureCredential is used for all services (managed
# identity in Azure, Azure CLI / VS Code locally).  No API keys needed.
# --------------------------------------------------------------------------

# --------------- database/ — Azure Cosmos DB ---------------
COSMOS_ENDPOINT=
COSMOS_DATABASE=ot-tag-registry

# --------------- Azure AI Foundry (shared by search/ and language/) ---------------
PROJECT_ENDPOINT=

# --------------- search/ — Azure AI Search + embeddings ---------------
SEARCH_ENDPOINT=
SEARCH_INDEX_NAME=golden-tags
PROJECT_EMBEDDING_DEPLOYMENT=

# --------------- language/ — Translator via Foundry ---------------
# Uses PROJECT_ENDPOINT above — no additional endpoint needed
